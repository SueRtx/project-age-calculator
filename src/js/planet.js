export default class HumanAge {
  constructor(ageNow, ageEnd) {
    this.ageNow = ageNow;
    this.ageEnd = ageEnd;

  }

 /* checkType() {
    if ( ) {
      return " ";
    } else if ( ) {
      return " ";
    }  else if ( ) {
      return " ";
    } else {
      return " ";
    }
  } */   
}